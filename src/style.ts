import { css } from "lit-element";

export default css`
  #card {
    display: grid;
    grid-gap: 1em;
    border: 1px solid #000;
    grid-template-columns: 1fr auto;
    align-items: center;
  }
`;
